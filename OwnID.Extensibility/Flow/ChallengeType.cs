namespace OwnID.Extensibility.Flow
{
    /// <summary>
    ///     Challenge type (login, link, etc.)
    /// </summary>
    public enum ChallengeType
    {
        Register,
        Login,
        Link,
        Recover
    }
}